# Assumptions and Limits

- Large raw datasets are intentionally excluded from git.
- Paths to heavy artifacts are documented in `manifiest/INVENTARIO_GRANDES_REFERENCIAS.tsv`.
- Parsing logic is centered on `parse_soft_v11.awk`, which may reject atypical table styles.
- GSE12906 is the validated pilot path for end-to-end processing.
